# Распределение ответственности

| Работа | Основные файлы | Статус | Что проверяется |
|---|---|---|---|
| Задача 1: меню и бизнес | `bot/menu.py`, `bot/business.py` | В процессе разработки | интерфейс `/start`, два бизнеса, профиль, настройки, поиск, избранное и связанная логика |
| Задача 2: модели и JSON | `models.py`, `storage.py` | В процессе разработки | интерфейсный контракт моделей, транзакции, JSON и разделение бизнесов |
| Интеграционный каркас | `main.py`, `config.py`, `bot/common.py`, `logic/catalog.py`, `logic/rules.py` | В процессе разработки | запуск, зависимости, FSM, сервисные методы и ошибки |
| Будущие Telegram-модули | `bot/services.py`, `bot/schedule.py`, `bot/calendar.py`, `bot/booking.py`, `bot/appointments.py`, `bot/notifications.py` | Запланировано | отдельные постановки и Pull Request |
| Будущая логика записей | `logic/availability.py`, `logic/booking.py`, `repository.py` | Запланировано | доступность, операции записей и контракт хранения |
| SQL Server | `database/`, `alembic/`, `tests/database/` | Запланировано | ORM, ограничения, миграции и транзакции |

Общие файлы не изменяются параллельно без согласования контракта. Участник работает в
своей ветке, запускает проверки и передаёт изменение через Pull Request. Основная ветка
`main` объединяется только после просмотра изменений и проверки сценария.
